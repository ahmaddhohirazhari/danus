import { AppLayout } from '@/components/layout/app-layout';
import { CreditScoreTool } from '@/components/credit-score-tool';

export default function Home() {
  return (
    <AppLayout>
      <CreditScoreTool />
    </AppLayout>
  );
}
